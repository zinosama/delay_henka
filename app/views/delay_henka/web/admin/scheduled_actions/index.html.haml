%h1 Scheduled Actions
%table.table
  %thead
    %tr
      %th Type
      %th Id
      %th Method
      %th Argument
      %th State
      %th Return Value
      %th Schedule At
      %th Time Zone
      -# TODO-BAC-2434 remove when backfilled to submitted_by_email
      %th Submitted By
      %th Submitted By Email
  %tbody
    - @actions.each do |action|
      %tr
        %td= action.actionable_type
        %td= action.actionable_id
        %td= action.method_name
        %td= action.argument
        %td= action.state
        %td= action.return_value
        %td= action.schedule_at
        %td= action.time_zone
        -# TODO-BAC-2434 remove when backfilled to submitted_by_email
        %td= action.submitted_by_id
        %td= action.submitted_by_email
