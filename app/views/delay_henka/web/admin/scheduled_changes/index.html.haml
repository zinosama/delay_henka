%h1 Scheduled Changes
%table.table
  %thead
    %tr
      %th Type
      %th Id
      %th Attribute
      %th State
      %th Old Value
      %th New Value
      %th Schedule At
      %th Time Zone
      -# TODO-BAC-2434 remove when backfilled to submitted_by_email
      %th Submitted By
      %th Submitted By Email
  %tbody
    - @changes.each do |change|
      %tr
        %td= change.changeable_type
        %td= change.changeable_id
        %td= change.attribute_name
        %td= change.state
        %td= change.old_value
        %td= change.new_value
        %td= change.schedule_at
        %td= change.time_zone
        -# TODO-BAC-2434 remove when backfilled to submitted_by_email
        %td= change.submitted_by_id
        %td= change.submitted_by_email
